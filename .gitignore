# Secrets — NEVER commit
secrets/
.env
*.env
API_KEYS.md
*_keys.md

# Live trading state (churns every spawn; not a durable record)
state/TRADE_STATE.md
TRADE_STATE.md

# Local Claude Code settings (personal; shared rules go in .claude/settings.json)
.claude/settings.local.json

# Cron scheduler runtime (session/local state, not a durable artifact)
.claude/scheduled_tasks.lock
.claude/scheduled_tasks.json

# Noise
.DS_Store
*.log
__pycache__/
.venv/
node_modules/
